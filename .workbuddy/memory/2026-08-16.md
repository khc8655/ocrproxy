# 2026-08-16

## OCRProxy 生产部署现状核查
- VM：腾讯云免费机 `ubuntu@124.223.35.23`，SSH 私钥 `tx_khc.pem`（项目根目录，600）。
- OS Ubuntu 24.04.4 LTS，内存 663M/3.6G，磁盘 5.9G/40G，uptime 10 天，状态健康。
- 架构：Caddy（*:80/443/9090 + 本地 2019 admin）反向代理 → OCRProxy uvicorn（127.0.0.1:8787，systemd `ocrproxy.service`）。
  - 对外 API 入口 = `*:9090`（Caddy）→ 8787。
  - 4 类虚拟接口：chat / embedding / reranker / ocr（知识库入库链路）。
  - 网站 www.khc6.cn（80/443）→ /var/www/ocrproxy/index.html（备案页"康康个人博客"，豫ICP备2026036006号，与本地 index.html 一致）。
  - 定时器：每分钟健康检查（ocrproxy-health.timer）、每日 04:00 重启（ocrproxy-restart.timer）。
- ⚠️ 疑点：`/health` 仅返回 `providers:["testprovider"]`，疑似测试/占位上游配置，需确认 4 类 candidate 是否指向有效真实 key、知识库入库链路是否实际可用。
- 配置落盘于 /opt/ocrproxy/config（ocrproxy 用户 700，加密存储），普通不可读。

## www.khc6.cn 经 EdgeOne 访问 522 排障
- 现象：浏览器访问 www.khc6.cn 打不开，EO 返回 522（回源失败）。
- 链路：DNS 已正确 CNAME 到 EdgeOne（www.khc6.cn → eo.dnse2.com → 43.174.x），EO 控制台回源配到主机 124.223.35.23。
- **根因：腾讯云安全组只放行 80、未放行 443**。外部 TCP 探测 80 通、443 超时；源站 Caddy 80/443(www) 应用层均正常（环回 200），故为云网络层拦截，非应用故障。
- 修复：①治本——安全组入站放行 TCP:443（0.0.0.0/0 或 EO 回源 IP 段）；②应急——EO 回源协议改 HTTP/80（源站 80 已通，绕过 443）。
- 注意：本地 `edgeone` CLI 是 Makers/Pages 命令行，不管理 CDN 回源（回源在 EO 边缘安全加速控制台）。裸域名 khc6.cn 无 A 记录，故打不开属正常。

## OCRProxy Web 管理面板安全风险核查（用户关注核心服务安全）
- OCRProxy = FastAPI 应用，管理面板为 SPA（"LLM Proxy 控制台"），路由：`/` 与 `/admin`（面板 HTML）、`/docs`、`/redoc`（Swagger/ReDoc）、`/api/admin/*`（管理 API）、`/v1/*`（代理 API）。
- **鉴权边界**：
  - `/v1/*`（含 chat/embeddings/rerank/ocr/models）需 `verify_proxy_auth`（Bearer PROXY_API_KEY 或 ADMIN_PASSWORD）。
  - `/api/admin/*`（config/stats/verify-key/test-candidate）需 `verify_admin_auth`。
  - ❌ **裸暴露无鉴权**：`/`（面板 UI）、`/admin`、`/docs`、`/redoc`、`/health`。
- **当前暴露面**：Caddy `:9090` 块反代 8787，但**无 TLS、无 basicauth、无 IP 限制** → 管理面板 UI / Swagger / admin API 全部明文挂在公网（*:9090）。
- 安全配置方向（待用户拍板，未执行）：
  - 对外 API：改走子域 `api.khc6.cn` + Caddy HTTPS:443 → 8787（仅放 /v1/* 等 API 路由，不放 / /admin /docs），弃用裸 9090。
  - 管理面板：方案A（最安全）仅经 WireGuard VPN / SSH 隧道访问 127.0.0.1:8787，完全不暴露公网；方案B（折中）走 `dash.khc6.cn` + Caddy `basicauth` + `remote_ip` allowlist。
  - 前提：腾讯云安全组须放行 443（同 522 排障根因）。
  - 可选应用层加固：给 `/`、`/admin` 加 `verify_admin_auth` 依赖（改 app/main.py）。
- 用户已有 WireGuard VPN + 阿里云聚合网关架构，方案A 与其既有一致、最稳。

## api.khc6.cn 面板登录 401 + 无提示排障（23:14）
- 现象：登录输入 key 无反应；控制台 `GET /api/admin/config` 401；webfonts 404。
- 链路验证：本地与经 EO 无 key 均 401、带正确 key 均 200、/health 200 → **链路（EO→9090→8787）与 Authorization 透传完全正常**，401 根因=登录输入的 key 与 .env 不符。
- 凭据位置：/opt/ocrproxy/.env（ocrproxy 700；ubuntu 可 sudo 免密读）。面板登录=ADMIN_PASSWORD(24位) 或 PROXY_API_KEY(40位)；agent 调 /v1/* 用 PROXY_API_KEY。**明文不写入记忆**。
- **UX bug 根因**：admin.html 的 .toast-container z-index:2000 < .login-overlay z-index:5000 → 登录失败 toast 被遮罩盖住，用户"看不到提示"（提示实际执行了）。修复：toast z-index 提到 6000+（1 行 CSS），可选加登录框内联错误。
- 静态字体缺失：/opt/ocrproxy/static/vendor/webfonts/ 不存在 → fa-solid-900.woff2/ttf 404（仅图标不显示，不影响功能）。
- 改 static/admin.html 即生效（FastAPI StaticFiles 直读磁盘，刷新浏览器即可，无需重启）。
- **✅ 已修复（23:16，用户确认「写吧」）**：① `.toast-container` z-index 2000→6000（登录失败 toast 不再被遮罩盖住）；② 登录框加 `#loginError` 内联红字提示（401 显示"密钥校验失败，请检查 API Key"，空输入提示"请输入管理员密钥"）；③ 下载 Font Awesome 6.4.0 的 fa-solid-900.woff2/ttf 补齐 `/opt/ocrproxy/static/vendor/webfonts/`（本地同步到 vm-app/static/vendor/webfonts/）。经 EO 实测：admin.html 含 loginError=2、woff2/ttf 200、wrong-key 401、correct-key 200。改动经 scp /tmp + sudo 落盘，owner ocrproxy。
- **✅ webfonts 全套补齐（23:17）**：admin.html 的 all.min.css 还引用 fa-regular-400 / fa-brands-400，补齐 4 文件（regular+brands 的 woff2/ttf），现 static/vendor/webfonts/ 共 6 文件（solid/regular/brands × woff2/ttf），经 EO 全部 200。

## 三项优化完成（23:50，git commit 2a9bc87，已部署线上并实测）
1. **前端**：移除 Font Awesome 依赖（all.min.css + webfonts），图标改**内联 SVG sprite**（25 symbols，`<use href="#fa-xxx">` 45 处，`.ic` 类 + `ic-spin` 动画），零字体请求；候选路由新增 **Key 多选**（checkbox 列表 `#candKeyList`，`saveCandidate` 批量 push，编辑模式首项覆盖+其余追加）。JS 语法 node --check 通过。
2. **后端**：KB chat 模式**写死快速**（非流式 + `chat_fast_timeout` 30s + 禁用思考），移除 `chat_fast_mode` 开关（proxy_routes.py）；Agent 模式保持完全透传（实测 tool calling 返回 `tool_calls`+`reasoning_content`）。已部署 /opt/ocrproxy 并重启，备份 .bak-20260816。
3. **稳定性测试全绿**（stab_test.py，真实 key 本地 8787）：models 200（9 模型）、chat-KB 200 快速、chat-agent 200 透传、chat-agent+tools 200 含 tool_calls、embedding 200 **dim=2560**（siliconflow Qwen3-Embedding-4B 原生维度，非 2048！）、rerank 200（4 结果）、ocr 200（sensenova-6.8-flash-lite 识别纯色图）。
- **⚠️ 注意**：embedding 输出 2560 维——若 khc 知识库（ocr.khc6.eu.cc 网关那套，索引 1024/2048 维）接入此端点，维度不匹配会入库失败，需确认使用场景。
- 线上配置（2026-08-16）：providers=siliconflow(4keys)/sensenova(7keys)/stepfun(2keys)；candidates=chat 6（sensenova deepseek-v4-flash×4 + stepfun step-3.7-flash×2）、embedding 3、reranker 2、ocr 3。health 已返回真实 providers（此前 testprovider 为旧配置缓存）。
- git：根目录仓库（vm-app 非独立仓），commit 2a9bc87（10 文件 +95/-78）。.workbuddy/memory 未提交（未加 gitignore，如要排除需加）；commit 作者为自动配置 xk@kanghongcandeMac-mini.local。

## Agent 模型中转独立配置完成（8-17 00:15，git 579cf1b，已部署实测）
- **需求**：Agent 中转模型独立于知识库 4 虚拟模型管理；/v1/models 不显示虚拟别名（入库示例代码直接写死模型名）。
- **后端** proxy_routes.py：Agent 模式改从独立 `agent_models` 配置节路由（不再依赖 candidates.chat）；`/v1/models` 只返回 agent_models 真实模型（移除 chat/embedding/reranker/ocr 4 个虚拟别名）。
- **前端** admin.html：新增「Agent 模型」独立 Tab（表格列表 + 添加/编辑/删除/排序/探活；Key 多选批量绑定，模态框 agentModelModal）；navItems 加 agent 项（icon server）；panel-agent；renderAgentModels 等函数。
- **配置迁移**（admin API 落盘）：agent_models=6（sensenova deepseek-v4-flash×4 + stepfun step-3.7-flash×2），从 chat 候选复制；chat 候选保留供 KB 用。
- **⚠️ 图标 bug 修复**：首版脚本把 fa-solid/fa-regular 前缀当图标名（45 处 `<use href="#fa-solid">` 全错）→ 回退 57074d9 重做；后又发现双 fa- 前缀（#fa-fa-key）→ sed 修复；补 6 个导航图标（chart-line/network-wired/shuffle/bug/sliders/server），sprite 31 symbols。
- **实测全绿**：/v1/models 经 EO 仅返回 [deepseek-v4-flash, step-3.7-flash]；Agent 透传含 tool_calls 200；KB chat/embedding(dim=2560)/rerank/ocr 全部 200；admin.html 含 Agent 面板。
- 备份：/opt/ocrproxy/app/proxy_routes.py.bak2-20260817、static/admin.html.bak2-20260817。
- git：commit 579cf1b（amend 后，3 文件 +209/-81 + 根 README）。

## api.khc6.cn 经 EdgeOne 回源 9090 安全方案（用户新目标：隐藏IP+免证书+加速）
- 目标：api.khc6.cn 经 EO 加速 + 免费证书，回源 VM:9090；隐藏源站 IP、免维护证书、用 EO 加速。
- 已查证（腾讯云官方文档 2026-08-16）：① EO 支持任意自定义回源端口（api→EO:443→回源 124.223.35.23:9090 可行）；② EO 免费 SSL 证书自动续期，源站无需证书；③ EO「源站防护」(安全防护>源站防护) 启用后给回源 IP 段列表，配到安全组入站白名单即仅 EO 能回源、隐藏源站；回源 IP 段不定期变更（提前14天通知，14天内须更新安全组）。
- **关键证据修正**：外部 TCP 9090 探测 **timed out / HTTP 000** → 当前 9090 对 0.0.0.0/0 不可达（安全组未放行公网），源站 IP 已部分隐藏；但 Caddy `:9090` 仍全量反代 8787（含 /、/admin、/docs、/redoc 无鉴权路由）。
- 落地要点（未执行，待「写吧」）：
  - EO 侧：添加 api.khc6.cn；源站 124.223.35.23；回源协议 **HTTP**、端口 **9090**；申请免费证书；启用源站防护；规则引擎配 API 不缓存。
  - 安全组：9090 入站改为【仅 EO 回源 IP 段】（勿放行 0.0.0.0/0）→ EO 能回源且外部直连超时=隐藏 IP。
  - Caddy 9090 块路由收敛：仅放行 /v1/*、/api/v1/*、/health、/v1/models；对 /、/admin、/docs、/redoc、/api/admin/* 返回 404，防经 EO 暴露管理面板。
  - 管理面板不暴露公网：经 WireGuard VPN / SSH 隧道(`ssh -L 8787:127.0.0.1:8787`) 访问 127.0.0.1:8787（8787 本地监听全路由）。
  - 坑：回源必须 HTTP:9090（非 HTTPS:443，否则 522）；9090 须保持 0.0.0.0 监听（不能改 127.0.0.1，EO 外部回源），靠安全组挡来源；EO 回源 IP 段变更须及时更新安全组。
  - **用户决策修正（21:15）**：用户指出面板「操作有认证」（/api/admin/* 需 ADMIN_PASSWORD，verify_admin_auth 已落地），质疑为何还要动 Caddy。事实：/（面板HTML）、/admin、/docs、/redoc 无 key 即可访问（仅看 UI/文档），但实际操作受 admin key 拦；/api/admin/* 受 verify_admin_auth 保护。**动 Caddy 屏蔽属可选加固（隐藏 UI/Swagger 可见性+收 admin API 攻击面），非必需。** 用户决定**不动 Caddy**。
- **⚠️ 重大更正（21:28）：免费版 EO 源站防护已下架，IP 白名单方案不可行。** 腾讯云官方公告（cloud.tencent.com/announce/detail/2153）：免费套餐自 2025-11-14 起逐步停用「源站防护」，停用后**不再提供固定回源 IP 网段**。故「安全组 9090 仅放 EO 回源段」在免费版下无法执行（无 CIDR 可填）。替代：①方案A 保持免费→安全组 9090 放行 0.0.0.0/0 让 EO 回源，靠 Bearer key+强随机 admin key 防护，源站 IP 扫描可见但用户访问仅见 EO 节点（与用户「面板有认证就够了」一致，最务实）；②方案B 真正隐藏 IP→升级 EO 付费套餐(个人版起)恢复源站防护。用户倾向免费，方案A 优先。DescribeOriginACL API 仅付费/已启用用户可调用。
